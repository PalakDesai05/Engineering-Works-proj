# Core web framework
flask>=3.0.0
flask-cors>=4.0.0

# Image processing
Pillow>=10.0.0
numpy>=1.26.0

# Face recognition (requires cmake + dlib)
# Windows: install cmake, then → pip install dlib face-recognition
# face-recognition>=1.3.0

# Lightweight alternative (no cmake needed, uncomment if preferred)
# mediapipe>=0.10.0
