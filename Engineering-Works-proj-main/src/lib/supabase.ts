// Supabase has been replaced by the Express API (src/lib/api.ts).
// This file is kept as a stub to avoid breaking any lingering imports.

export const supabase = null;
export type Worker = import('./api').Worker;
export type Attendance = import('./api').AttendanceRow;
